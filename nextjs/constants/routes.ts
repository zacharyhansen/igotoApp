const LOGIN = '/login';
const HOME = '/home';
const PROFILE = '/profile';
const DASHBOARD = '/dashboard';

export const routes = {
  LOGIN: LOGIN,
  PROFILE: HOME + PROFILE,
  DASHBOARD: HOME + DASHBOARD
};
